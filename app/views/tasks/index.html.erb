<h1>Tasks</h1>
<ul class="tasks" id="tasks">
  <%= render @tasks %>
</ul>

<%= form_for [@task, @tasks.new], remote: true do |f| %>
  <%= f.text_field :name, placeholder: "New Task", class: 'input-task' %>
  <%= f.submit "Add", class: 'button button-add' %>
<% end %>